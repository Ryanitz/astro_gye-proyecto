---
import datos from "../data/familias_montos.json";

// Obtener nombres de familias desde el JSON, excluyendo "tipo de cambio avg"
const donatingFamilies = Object.keys(datos).filter(
  (name) => name !== "tipo de cambio avg"
);

// Ordenar familias alfabéticamente
const sortedFamilies = donatingFamilies.sort((a, b) => a.localeCompare(b));
---

<section
  class="bg-gradient-to-br from-blue-50 to-white shadow-lg rounded-2xl p-10 animate-fade-in"
>
  <h2 class="text-4xl font-bold text-center text-gray-900 mb-2">
    Familias que colaboraron 👇
  </h2>
  <h3 class="text-xl font-semibold text-center mb-8">
    Ya se sumaron {donatingFamilies.length} familias
  </h3>

  <div
    class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4"
  >
    {
      sortedFamilies.map((name) => (
        <div class="text-center p-6 bg-gray-100 rounded-xl shadow-lg transition-all transform hover:scale-105 hover:bg-gray-200 hover:shadow-xl">
          <h3 class="text-2xl capitalize font-semibold text-gray-900">
            {name}
          </h3>
        </div>
      ))
    }
  </div>

  <div class="mt-10">
    <h3 class="text-3xl font-bold text-center text-gray-900">
      ¡Muchas gracias! 🙌
    </h3>
    <p class="text-lg text-center text-gray-700 mt-4">
      Agradecemos especialmente a las <strong>Categorías 2012 y 2008</strong> por
      unirse y colaborar. Tambien queremos agradecer a la familia Waldman por donar
      las luces del Express. Juntos hacemos posible algo grande.
    </p>
  </div>
</section>
